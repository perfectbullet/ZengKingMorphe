{
  "model": "${MODEL_NAME}",
  "api_base": "${VLLM_API_BASE}",
  "tools": {
    "python": {
      "enabled": true,
      "timeout": 30,
      "work_dir": "/workspace",
      "sandbox": "${PYTHON_EXECUTOR_MODE}" == "sandbox"
    }
  },
  "system_prompt": "Please integrate natural language reasoning with programs to solve the problem above, and put your final answer within boxed{}."
}