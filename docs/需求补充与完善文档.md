# 数字员工项目需求补充与完善文档

## 文档信息
- **版本**：v1.0
- **创建日期**：2025-12-13
- **目标**：基于原始需求进行合理补充和完善

---

## 一、原始需求回顾

### 1. 核心AI能力需求

#### 对话引擎模块
1. 对话接口
2. 多知识库路由
3. 多轮对话管理
4. 流式响应
5. 敏感词、敏感问题拦截

#### 知识库系统
1. 多知识库管理
2. RAG文档库管理（创建、解析、查询）
3. FAQ问答库
4. 文档处理管线
5. 混合检索策略
6. 知识库质量管理
7. FAQ特殊功能
8. 其他必要需求

#### 对话记录管理
1. 对话记录查看、搜索、导出、统计

#### 词库管理
1. 专业词库或专有名称库、行业领域专有名称库
2. 敏感词库（涉及毒品、暴力、性犯罪的内容与词汇）

#### 意图识别模块
待补充

---

## 二、需求补充与完善

### 1. 对话引擎模块 - 补充需求

#### 1.1 对话接口（扩展）
**原需求**：对话接口

**补充需求**：
- **REST API接口**：
  - `POST /api/chat/message` - 同步对话接口
  - `POST /api/chat/stream` - 流式对话接口
  - `GET /api/chat/session/{id}` - 获取会话信息
  - `DELETE /api/chat/session/{id}` - 结束会话

- **WebSocket接口**：
  - `/ws/chat` - 实时双向通信
  - 支持断线重连
  - 心跳检测机制
  - 消息确认机制

- **接口鉴权与安全**：
  - JWT Token 认证
  - API Key 认证（供第三方集成）
  - 请求签名验证
  - IP白名单限制

- **接口限流**：
  - 每用户每分钟最大请求数
  - 并发会话数限制
  - 令牌桶算法实现

#### 1.2 多知识库路由（扩展）
**原需求**：多知识库路由

**补充需求**：
- **智能路由策略**：
  - 基于意图的自动路由
  - 基于用户画像的路由（VIP用户优先匹配高质量知识库）
  - 基于问题分类的路由
  - 基于时间的路由（工作时间vs非工作时间）
  
- **多路召回机制**：
  - 并行查询多个知识库
  - 结果融合与去重
  - 加权排序算法
  - 知识库优先级配置

- **路由规则引擎**：
  - 可视化规则配置
  - 规则版本管理
  - A/B测试支持
  - 路由效果追踪

#### 1.3 多轮对话管理（扩展）
**原需求**：多轮对话管理

**补充需求**：
- **上下文管理**：
  - 会话上下文存储（最近N轮对话）
  - 上下文窗口大小配置
  - 上下文摘要与压缩（长对话场景）
  - 跨会话上下文传承（识别老用户）

- **对话状态机**：
  - 问题澄清状态
  - 信息收集状态
  - 任务执行状态
  - 确认反馈状态
  - 结束会话状态

- **多轮对话策略**：
  - 缺失信息主动追问
  - 多意图消歧
  - 槽位填充（Slot Filling）
  - 对话流程跳转

- **会话管理**：
  - 会话超时自动结束
  - 会话恢复机制
  - 会话克隆（用于调试）
  - 会话导出与分享

#### 1.4 流式响应（扩展）
**原需求**：流式响应

**补充需求**：
- **流式输出控制**：
  - 可配置的流式输出速度
  - 按句子/段落分块输出
  - 支持中断流式输出
  - 流式输出错误重试

- **流式响应优化**：
  - 首Token时延优化（TTFT - Time To First Token）
  - 流式缓存机制
  - 网络带宽自适应
  - 移动端流式优化

- **流式响应格式**：
  - Server-Sent Events (SSE)
  - WebSocket流式
  - Chunked Transfer Encoding
  - 自定义流式协议

#### 1.5 敏感词、敏感问题拦截（扩展）
**原需求**：敏感词、敏感问题拦截

**补充需求**：
- **敏感内容检测层级**：
  - **用户输入检测**：
    - 敏感词匹配（精确、模糊、正则）
    - 敏感短语检测
    - 敏感语义识别（基于模型）
    - 变体词检测（火星文、谐音等）
  
  - **AI输出检测**：
    - 生成内容安全审核
    - 敏感信息泄露检测
    - 幻觉内容检测
    - 政治倾向检测

- **拦截策略**：
  - **严重级别**（Critical）：直接拒绝服务，返回警告
  - **警告级别**（Warning）：替换敏感词，记录日志
  - **提示级别**（Notice）：仅记录日志，不拦截

- **拦截后处理**：
  - 返回友好的拒绝话术
  - 提供申诉通道
  - 人工审核机制
  - 黑名单管理（恶意用户）

- **合规性保障**：
  - 网络安全法合规
  - 数据安全法合规
  - 个人信息保护法合规
  - 行业特定合规（金融、医疗等）

#### 1.6 新增需求：对话质量保障

- **答案置信度评估**：
  - 检索结果相似度评分
  - LLM生成质量评分
  - 综合置信度计算
  - 低置信度处理策略（转人工、拒绝回答）

- **答案一致性校验**：
  - 同一问题多次回答一致性检查
  - 跨会话答案一致性
  - 答案与知识库一致性
  - 答案变更追踪

- **答案质量监控**：
  - 用户满意度反馈（👍/👎）
  - 答案点击率
  - 追问率分析
  - 转人工率统计

#### 1.7 新增需求：对话个性化

- **用户画像管理**：
  - 用户基础信息
  - 历史对话偏好
  - 专业领域标签
  - 沟通风格偏好

- **个性化对话策略**：
  - 基于用户等级的服务差异化
  - 老用户快速识别与问候
  - 用户偏好记忆（语言风格、详细程度）
  - 个性化推荐（热门问题、相关文档）

#### 1.8 新增需求：对话异常处理

- **无法理解用户问题**：
  - 返回引导话术
  - 提供相似问题建议
  - 引导用户换个说法

- **知识库无匹配**：
  - 返回"暂无相关信息"话术
  - 提供人工客服入口
  - 记录缺失知识点

- **LLM调用失败**：
  - 自动重试机制
  - 降级到规则引擎
  - 返回友好错误提示

- **对话超时处理**：
  - 用户长时间无响应自动结束
  - 超时前提醒用户
  - 会话保存与恢复

---

### 2. 知识库系统 - 补充需求

#### 2.1 多知识库管理（扩展）

**补充需求**：
- **知识库生命周期管理**：
  - 创建、启用、禁用、归档、删除
  - 知识库版本管理
  - 知识库克隆与复制
  - 知识库导入导出

- **知识库权限管理**：
  - 知识库所有者
  - 编辑者（可添加/修改内容）
  - 审核者（审批内容变更）
  - 查看者（只读）

- **知识库分类与标签**：
  - 业务线分类（产品、技术、售后等）
  - 行业分类（金融、医疗、教育等）
  - 优先级标签（高、中、低）
  - 自定义标签

- **知识库监控**：
  - 知识库命中率统计
  - 知识库召回率分析
  - 知识库更新频率
  - 知识库质量评分

#### 2.2 RAG文档库管理（扩展）

**补充需求**：
- **文档上传增强**：
  - 支持格式：PDF、Word、TXT、Markdown、HTML、CSV、Excel、PPT
  - 批量上传（最多100个文件）
  - 大文件上传（支持分片上传，最大500MB）
  - URL导入（爬取网页内容）
  - API导入（第三方系统对接）

- **文档解析优化**：
  - PDF文本提取（含OCR支持）
  - Word文档解析（保留格式）
  - 表格识别与结构化
  - 图片识别与描述生成
  - 代码块识别与高亮

- **文档预处理**：
  - 去除页眉页脚
  - 去除水印
  - 噪声过滤（广告、导航栏等）
  - 编码自动识别
  - 文档去重

- **文档分块策略（Chunking）**：
  - 固定大小分块（如512 tokens）
  - 按段落分块
  - 按语义分块（使用语义分割模型）
  - 重叠分块（Overlap）
  - 分块大小可配置

- **文档向量化**：
  - Embedding模型选择（text-embedding-3-small/large）
  - 批量向量化
  - 向量维度配置
  - 向量缓存机制
  - 向量更新策略

- **知识片段管理**：
  - 手动添加知识片段
  - 编辑知识片段内容
  - 删除无效片段
  - 片段合并与拆分
  - 片段元数据管理（来源、页码、章节等）

- **文档查询优化**：
  - 相似度阈值配置
  - Top K配置
  - 重排序（Rerank）
  - 上下文窗口扩展
  - 查询结果高亮

#### 2.3 FAQ问答库（扩展）

**补充需求**：
- **FAQ结构化管理**：
  - 标准问题
  - 标准答案（支持富文本、Markdown）
  - 相似问题列表
  - 关键词标签
  - FAQ分类
  - 关联FAQ推荐

- **FAQ批量管理**：
  - Excel批量导入
  - 模板下载
  - 批量编辑
  - 批量删除
  - 批量导出

- **FAQ智能辅助**：
  - 相似问题自动聚类
  - 相似问题推荐
  - 关键词自动提取
  - 答案质量评估
  - 冲突检测（相似问题不同答案）

- **FAQ版本管理**：
  - 答案历史版本
  - 版本对比
  - 版本回滚
  - 变更审批流程

- **FAQ效果追踪**：
  - FAQ命中次数
  - 用户满意度
  - 答案修改历史
  - 相关追问统计

#### 2.4 文档处理管线（扩展）

**补充需求**：
- **处理流程**：
  ```
  文档上传 → 格式验证 → 文本提取 → 预处理 → 分块 → 向量化 → 存储
  ```

- **异步处理**：
  - 任务队列（Celery/RQ）
  - 处理进度查询
  - 失败重试机制
  - 处理日志记录

- **质量检查**：
  - 文本提取质量评估
  - 分块质量检查
  - 向量化成功率
  - 异常文档标记

- **管线配置**：
  - 可视化配置界面
  - 预设处理模板
  - 自定义处理步骤
  - 管线性能监控

#### 2.5 混合检索策略（扩展）

**补充需求**：
- **多路检索**：
  - **向量检索**（Chroma）：语义相似度匹配
  - **关键词检索**（ElasticSearch）：精确关键词匹配
  - **全文检索**（ElasticSearch）：BM25算法
  - **专业词库增强**：实体识别、同义词扩展

- **检索融合算法**：
  - **RRF（Reciprocal Rank Fusion）**：
    ```python
    score(d) = Σ 1 / (k + rank_i(d))
    ```
  - **加权融合**：
    ```python
    score(d) = w1 * vector_score + w2 * keyword_score
    ```
  - **CombSUM/CombMNZ**：传统融合算法

- **检索优化**：
  - 查询扩展（Query Expansion）
  - 查询重写（Query Rewriting）
  - 同义词替换
  - 拼写纠错
  - 停用词过滤

- **重排序（Rerank）**：
  - 使用专用重排序模型（bge-reranker）
  - 基于上下文的重排序
  - 基于用户偏好的重排序
  - 多样性保证（避免重复结果）

#### 2.6 知识库质量管理（扩展）

**补充需求**：
- **质量评估指标**：
  - **覆盖率**：知识库能回答的问题比例
  - **准确率**：回答正确的比例
  - **召回率**：相关文档被检索出的比例
  - **精确率**：检索结果中相关文档的比例
  - **平均响应时间**

- **质量监控**：
  - 低质量内容检测（重复、无效、过时）
  - 知识缺口分析（用户问到但无答案的问题）
  - 知识冲突检测（同一问题不同答案）
  - 知识时效性检查

- **质量改进**：
  - 自动化质量报告
  - 质量改进建议
  - 知识补充提醒
  - 知识更新提醒

- **质量审核**：
  - 新增内容审核
  - 修改内容审核
  - 定期复审机制
  - 审核工作流

#### 2.7 FAQ特殊功能（扩展）

**补充需求**：
- **动态FAQ**：
  - 答案中包含变量（如用户名、订单号）
  - 答案模板配置
  - 实时数据查询（调用API获取最新数据）

- **条件FAQ**：
  - 基于条件的不同答案（如VIP用户vs普通用户）
  - 时间条件（工作日vs周末）
  - 地域条件（不同地区不同答案）

- **多媒体FAQ**：
  - 答案包含图片
  - 答案包含视频链接
  - 答案包含文档附件
  - 答案包含操作步骤截图

- **FAQ推荐**：
  - 相关FAQ推荐
  - 热门FAQ展示
  - 基于用户画像的FAQ推荐

#### 2.8 其他必要需求（新增）

- **知识库同步**：
  - MongoDB → Chroma向量库自动同步
  - MongoDB → ElasticSearch索引自动同步
  - 同步失败告警与重试

- **知识库备份与恢复**：
  - 定期自动备份
  - 备份版本管理
  - 一键恢复功能
  - 灾难恢复方案

- **知识库测试**：
  - 测试问题集管理
  - 批量测试执行
  - 测试结果对比
  - 回归测试

- **知识库文档化**：
  - 知识库使用说明
  - 最佳实践文档
  - FAQ编写规范
  - 质量标准文档

---

### 3. 对话记录管理 - 补充需求

#### 3.1 对话记录查看（原需求）
保持原需求不变

#### 3.2 对话记录搜索（原需求）
保持原需求不变

#### 3.3 对话记录导出（原需求）
保持原需求不变

#### 3.4 对话记录统计（原需求）
保持原需求不变

#### 3.5 对话记录筛选（新增）

**补充需求**：
- **筛选维度**：
  - 时间范围（今天、昨天、最近7天、最近30天、自定义）
  - 用户ID/用户名
  - 会话状态（进行中、已完成、超时）
  - 意图类型
  - 知识库来源
  - 评价状态（好评、差评、无评价）
  - 是否转人工
  - 对话轮次范围

- **高级筛选**：
  - 多条件组合筛选
  - 筛选条件保存
  - 快捷筛选预设

#### 3.6 对话数据分析（新增）

**补充需求**：
- **用户行为分析**：
  - 活跃用户统计
  - 用户留存率
  - 用户流失率
  - 用户生命周期分析

- **对话质量分析**：
  - 平均满意度趋势
  - 问题解决率
  - 首次解决率（FCR - First Contact Resolution）
  - 平均处理时长（AHT - Average Handling Time）

- **知识库效能分析**：
  - 各知识库命中率对比
  - 知识缺口Top 10
  - 低质量回答Top 10
  - 知识库使用趋势

- **意图分析**：
  - 意图分布统计
  - 意图识别准确率
  - 新增意图发现
  - 意图流转分析

- **时间维度分析**：
  - 按小时统计（找出高峰时段）
  - 按星期统计（工作日vs周末）
  - 按月份统计（季节性趋势）

#### 3.7 对话记录审计（新增）

**补充需求**：
- **敏感对话标记**：
  - 包含敏感词的对话
  - 用户投诉对话
  - 异常对话（如恶意测试）

- **合规性审计**：
  - 对话内容合规性检查
  - 敏感信息泄露检查
  - 审计日志记录

- **质量抽检**：
  - 随机抽样对话
  - 质量评分
  - 问题标注
  - 改进建议

#### 3.8 对话记录回放（新增）

**补充需求**：
- **可视化回放**：
  - 按时间轴展示完整对话
  - 高亮关键信息（意图、实体、知识库匹配）
  - 显示AI决策过程（检索结果、置信度）

- **调试功能**：
  - 单步回放
  - 重新执行某轮对话
  - 修改参数后重新测试

---

### 4. 词库管理 - 补充需求

#### 4.1 专业词库/专有名称库（扩展）

**补充需求**：
- **词库分类**：
  - **产品名称词库**：公司产品、功能模块名称
  - **人名词库**：公司员工、合作伙伴
  - **地名词库**：办公地点、服务区域
  - **行业术语词库**：专业术语、缩写
  - **组织机构词库**：部门、团队名称

- **词条结构**：
  ```json
  {
    "term": "数字员工平台",
    "synonyms": ["智能客服", "AI助手"],
    "category": "产品",
    "weight": 1.5,
    "pinyin": "shuzi yuangong pingtai",
    "abbreviation": "SYGPT",
    "description": "企业级AI对话助手平台"
  }
  ```

- **词库功能**：
  - 同义词配置（多对一映射）
  - 词条权重设置（影响检索排序）
  - 拼音索引（支持拼音搜索）
  - 缩写映射
  - 词条描述

- **词库增强检索**：
  - 查询扩展（用同义词扩展查询）
  - 实体识别（NER - Named Entity Recognition）
  - 关键词提取
  - 拼写纠错

- **词库版本管理**：
  - 版本号自动管理
  - 版本差异对比
  - 版本回滚
  - 变更日志

- **词库权限管理**：
  - 词库所有者
  - 编辑者（可添加/修改词条）
  - 审核者（审批变更）
  - 查看者（只读）

- **词库导入导出**：
  - CSV格式导入导出
  - JSON格式导入导出
  - Excel格式导入导出
  - 模板下载

#### 4.2 敏感词库（扩展）

**补充需求**：
- **敏感词分类**：
  - **政治敏感词**：政治人物、敏感事件
  - **违法违规词**：毒品、赌博、诈骗、非法交易
  - **色情暴力词**：色情内容、暴力内容
  - **歧视词汇**：种族歧视、性别歧视
  - **广告营销词**：垃圾广告、违规营销
  - **隐私敏感词**：身份证号、手机号、银行卡号
  - **自定义敏感词**：企业特定敏感词

- **敏感词分级**：
  - **严重级别**（Critical）：直接拒绝服务
  - **警告级别**（Warning）：替换或删除
  - **提示级别**（Notice）：仅记录日志

- **匹配策略**：
  - **精确匹配**：完全匹配敏感词
  - **模糊匹配**：包含敏感词
  - **正则匹配**：支持正则表达式
  - **语义匹配**：基于语义相似度

- **敏感词处理动作**：
  - **阻止**（Block）：拒绝请求，返回错误
  - **替换**（Replace）：替换为***或其他字符
  - **警告**（Warn）：返回警告信息，记录日志
  - **仅记录**（Log）：不拦截，仅记录

- **变体检测**：
  - 拼音变体（如"du品" → "毒品"）
  - 火星文变体（如"赌!博" → "赌博"）
  - 谐音变体（如"du博" → "赌博"）
  - 英文缩写变体

- **敏感词白名单**：
  - 例外词汇（如"白雪"不是"白粉"）
  - 上下文白名单（特定语境下允许）

- **敏感词管理**：
  - 敏感词批量导入
  - 敏感词分类管理
  - 敏感词定期更新
  - 敏感词命中统计
  - 误拦截反馈机制

#### 4.3 停用词库（新增）

**补充需求**：
- **停用词作用**：
  - 检索时过滤无意义词汇（如"的"、"了"、"吗"）
  - 提高检索准确性
  - 减少向量化噪声

- **停用词分类**：
  - 通用停用词（中文常见虚词）
  - 领域停用词（特定领域无意义词）
  - 自定义停用词

#### 4.4 同义词库（新增）

**补充需求**：
- **同义词作用**：
  - 查询扩展（用户说"手机"也能匹配"移动电话"）
  - 提高召回率
  - 统一术语表达

- **同义词关系**：
  - 完全同义（如"手机" ≈ "移动电话"）
  - 上下位关系（如"苹果手机" → "手机"）
  - 相关词（如"密码" ↔ "账号"）

---

### 5. 意图识别模块 - 补充需求

#### 5.1 意图定义与管理

**补充需求**：
- **意图结构**：
  ```json
  {
    "intent_id": "password_reset",
    "name": "密码重置",
    "description": "用户想要重置或找回密码",
    "examples": [
      "忘记密码了",
      "如何重置密码",
      "密码找不回来了"
    ],
    "entities": [
      {
        "name": "account_type",
        "type": "enum",
        "values": ["邮箱", "手机", "用户名"]
      }
    ],
    "action": {
      "type": "search_faq",
      "params": {
        "category": "账户管理"
      }
    },
    "priority": 5,
    "status": "active"
  }
  ```

- **意图分类**：
  - 信息查询类（如"产品价格"）
  - 任务执行类（如"创建订单"）
  - 问题解决类（如"报修"）
  - 闲聊类（如"你好"）

- **意图层级**：
  - 一级意图（如"账户相关"）
  - 二级意图（如"密码重置"）
  - 三级意图（如"邮箱密码重置"）

#### 5.2 实体提取（NER）

**补充需求**：
- **实体类型**：
  - **系统实体**：时间、日期、数字、金额、电话、邮箱
  - **自定义实体**：产品名、订单号、用户ID、地址

- **实体提取方法**：
  - 基于规则（正则表达式）
  - 基于词典（专业词库）
  - 基于模型（NER模型）
  - 基于LLM（Prompt提取）

- **槽位填充（Slot Filling）**：
  - 识别缺失的必填槽位
  - 主动追问补充信息
  - 槽位验证（格式、范围）

#### 5.3 意图识别策略

**补充需求**：
- **识别方法**：
  - **基于规则**：关键词匹配
  - **基于分类模型**：训练意图分类器
  - **基于LLM**：Prompt引导识别（推荐）

- **LLM意图识别Prompt模板**：
  ```python
  prompt = f"""
  用户问题：{user_query}
  
  上下文：{context}
  
  可选意图列表：
  {format_intents(intent_list)}
  
  请分析用户问题，返回最匹配的意图ID和置信度（0-1）。
  
  返回格式：
  {{
    "intent_id": "xxx",
    "confidence": 0.95,
    "entities": {{...}}
  }}
  """
  ```

- **多意图处理**：
  - 识别多个意图（如"查询订单并申请退款"）
  - 意图优先级排序
  - 意图消歧（如"苹果"是水果还是手机？）

- **意图置信度阈值**：
  - 高置信度（>0.85）：直接执行
  - 中等置信度（0.65-0.85）：确认后执行
  - 低置信度（<0.65）：返回"无法理解"

#### 5.4 意图识别优化

**补充需求**：
- **意图调优工具**：
  - 意图描述生成（LLM辅助）
  - 意图示例扩展（自动生成相似问题）
  - 意图冲突检测（检测意图定义重叠）
  - 意图识别测试（批量测试准确率）

- **意图识别监控**：
  - 意图识别准确率
  - 各意图触发频率
  - 未识别意图统计
  - 意图识别耗时

- **意图识别改进**：
  - 从对话记录中发现新意图
  - 意图示例自动补充
  - 低准确率意图优化建议

#### 5.5 上下文意图识别

**补充需求**：
- **上下文影响**：
  - 前置对话影响意图判断
  - 用户画像影响意图判断
  - 会话状态影响意图判断

- **意图切换检测**：
  - 识别用户切换话题
  - 保存前一话题上下文
  - 支持话题回退

#### 5.6 意图路由

**补充需求**：
- **意图-动作映射**：
  - 意图 → 知识库检索
  - 意图 → 插件调用
  - 意图 → 工作流触发
  - 意图 → 转人工

- **意图路由规则**：
  ```json
  {
    "intent_id": "order_query",
    "actions": [
      {
        "type": "plugin_call",
        "plugin": "order_system",
        "method": "query_order",
        "params": ["order_id"]
      },
      {
        "type": "search_kb",
        "kb_ids": ["kb_order_faq"],
        "fallback": true
      }
    ]
  }
  ```

---

## 三、技术架构补充

### 1. LangGraph工作流设计

**LangGraph在项目中的应用**：

```python
from langgraph.graph import StateGraph, END
from typing import TypedDict, Annotated
from operator import add

# 定义对话状态
class ConversationState(TypedDict):
    messages: Annotated[list, add]
    user_query: str
    user_id: str
    session_id: str
    intent: str
    entities: dict
    retrieved_docs: list
    final_answer: str
    confidence: float
    context: dict

# 构建对话图
def build_conversation_graph():
    graph = StateGraph(ConversationState)
    
    # 添加节点（各个处理步骤）
    graph.add_node("input_validation", validate_input)
    graph.add_node("sensitive_check", check_sensitive_words)
    graph.add_node("intent_recognition", recognize_intent)
    graph.add_node("entity_extraction", extract_entities)
    graph.add_node("knowledge_routing", route_to_knowledge)
    graph.add_node("hybrid_retrieval", hybrid_search)
    graph.add_node("context_enhancement", enhance_with_context)
    graph.add_node("answer_generation", generate_answer)
    graph.add_node("quality_check", check_answer_quality)
    graph.add_node("output_filter", filter_output)
    graph.add_node("save_conversation", save_to_db)
    
    # 添加边（工作流程）
    graph.add_edge("input_validation", "sensitive_check")
    
    # 条件边：敏感词检测通过则继续，否则结束
    graph.add_conditional_edges(
        "sensitive_check",
        lambda state: "continue" if not state.get("has_sensitive") else "reject",
        {
            "continue": "intent_recognition",
            "reject": END
        }
    )
    
    graph.add_edge("intent_recognition", "entity_extraction")
    graph.add_edge("entity_extraction", "knowledge_routing")
    graph.add_edge("knowledge_routing", "hybrid_retrieval")
    graph.add_edge("hybrid_retrieval", "context_enhancement")
    graph.add_edge("context_enhancement", "answer_generation")
    graph.add_edge("answer_generation", "quality_check")
    
    # 条件边：质量检查通过则继续，否则降级处理
    graph.add_conditional_edges(
        "quality_check",
        lambda state: "pass" if state.get("confidence", 0) > 0.7 else "fallback",
        {
            "pass": "output_filter",
            "fallback": "answer_generation"  # 重新生成或返回默认回答
        }
    )
    
    graph.add_edge("output_filter", "save_conversation")
    graph.add_edge("save_conversation", END)
    
    # 设置入口
    graph.set_entry_point("input_validation")
    
    return graph.compile()
```

### 2. MongoDB数据模型补充

**新增集合**：

```javascript
// 用户画像集合
{
  collection: "user_profiles",
  schema: {
    user_id: String,
    nickname: String,
    preferences: {
      language: String,
      tone: String, // formal, casual, professional
      detail_level: String // brief, moderate, detailed
    },
    tags: Array,
    conversation_count: Number,
    satisfaction_avg: Number,
    created_at: ISODate,
    updated_at: ISODate
  }
}

// 意图识别日志集合
{
  collection: "intent_logs",
  schema: {
    log_id: String,
    conversation_id: String,
    user_query: String,
    recognized_intent: String,
    confidence: Number,
    entities: Object,
    timestamp: ISODate
  }
}

// 知识库质量评估集合
{
  collection: "kb_quality_metrics",
  schema: {
    kb_id: String,
    date: String,
    hit_rate: Number,
    recall_rate: Number,
    precision_rate: Number,
    avg_response_time: Number,
    total_queries: Number
  }
}
```

### 3. Chroma向量库设计补充

**集合配置**：

```python
import chromadb
from chromadb.config import Settings

client = chromadb.Client(Settings(
    chroma_db_impl="duckdb+parquet",
    persist_directory="./chroma_db"
))

# FAQ集合
faq_collection = client.create_collection(
    name="faq_knowledge",
    metadata={
        "description": "FAQ问答库",
        "embedding_model": "text-embedding-3-small"
    },
    embedding_function=openai_ef
)

# RAG文档集合
doc_collection = client.create_collection(
    name="rag_documents",
    metadata={
        "description": "RAG文档库",
        "embedding_model": "text-embedding-3-small"
    },
    embedding_function=openai_ef
)

# 专业词库集合（用于语义相似词查找）
dict_collection = client.create_collection(
    name="custom_dictionary",
    metadata={
        "description": "专业词库",
        "embedding_model": "text-embedding-3-small"
    },
    embedding_function=openai_ef
)
```

### 4. ElasticSearch索引设计补充

**索引配置**：

```json
{
  "settings": {
    "analysis": {
      "analyzer": {
        "ik_max_word_analyzer": {
          "type": "custom",
          "tokenizer": "ik_max_word",
          "filter": ["lowercase", "stop"]
        },
        "ik_smart_analyzer": {
          "type": "custom",
          "tokenizer": "ik_smart",
          "filter": ["lowercase", "stop"]
        }
      }
    },
    "number_of_shards": 3,
    "number_of_replicas": 1
  },
  "mappings": {
    "properties": {
      "faq_id": {"type": "keyword"},
      "question": {
        "type": "text",
        "analyzer": "ik_max_word_analyzer",
        "search_analyzer": "ik_smart_analyzer",
        "fields": {
          "keyword": {"type": "keyword"}
        }
      },
      "answer": {
        "type": "text",
        "analyzer": "ik_max_word_analyzer"
      },
      "keywords": {
        "type": "keyword"
      },
      "category": {"type": "keyword"},
      "created_at": {"type": "date"},
      "boost_score": {"type": "float"}
    }
  }
}
```

---

## 四、非功能性需求补充

### 1. 性能要求

- **响应时间**：
  - API响应时间 < 200ms（不含LLM调用）
  - LLM流式响应首Token时延 < 2s
  - 向量检索时间 < 100ms
  - 全文检索时间 < 50ms

- **并发能力**：
  - 支持1000 QPS（每秒查询数）
  - 支持10000并发WebSocket连接
  - 水平扩展能力

- **可用性**：
  - 系统可用性 > 99.9%
  - LLM降级方案（主模型不可用时切换备用模型）
  - 数据库主从备份

### 2. 安全要求

- **数据安全**：
  - 敏感数据加密存储
  - API密钥加密管理
  - 用户数据隔离
  - 定期安全审计

- **访问控制**：
  - 基于角色的权限管理（RBAC）
  - JWT Token认证
  - API密钥认证
  - IP白名单

- **内容安全**：
  - 敏感词过滤
  - 违规内容拦截
  - 用户输入验证
  - 输出内容审核

### 3. 可维护性要求

- **日志管理**：
  - 结构化日志
  - 日志级别（DEBUG/INFO/WARNING/ERROR）
  - 日志归档与清理
  - 日志分析与检索

- **监控告警**：
  - 系统性能监控
  - 错误率监控
  - 业务指标监控
  - 告警通知（邮件、钉钉、企业微信）

- **可观测性**：
  - 链路追踪（Tracing）
  - 指标收集（Metrics）
  - 日志聚合（Logging）

### 4. 扩展性要求

- **模块化设计**：
  - 松耦合架构
  - 插件化机制
  - 微服务架构（可选）

- **配置化**：
  - 配置与代码分离
  - 配置热更新
  - 多环境配置（开发、测试、生产）

---

## 五、开发优先级与里程碑

### Phase 1（2周）：核心基础能力

**目标**：搭建基础服务框架，实现基本对话能力

- [ ] FastAPI服务框架搭建
  - REST API接口
  - WebSocket接口
  - 鉴权中间件
  - 错误处理中间件
  
- [ ] LangGraph工作流搭建
  - 对话状态定义
  - 基础节点实现
  - 工作流编排

- [ ] 对话管理服务
  - 会话创建与管理
  - 上下文存储（MongoDB）
  - 流式响应

- [ ] 基础LLM集成
  - OpenAI API集成
  - 流式调用
  - 错误处理与重试

- [ ] 数据库初始化
  - MongoDB集合创建
  - Chroma集合创建
  - ElasticSearch索引创建

**交付物**：
- 可运行的FastAPI服务
- 支持基本对话的WebSocket接口
- LangGraph工作流框架

### Phase 2（2周）：知识库与检索

**目标**：实现知识库管理和混合检索策略

- [ ] 知识库管理服务
  - 知识库CRUD接口
  - 知识库权限管理
  
- [ ] FAQ问答库
  - FAQ数据结构设计
  - FAQ批量导入
  - FAQ向量化（Chroma）
  - FAQ全文索引（ElasticSearch）

- [ ] RAG文档库
  - 文档上传接口
  - 文档解析（PDF、Word、Markdown）
  - 文档分块
  - 文档向量化

- [ ] 混合检索策略
  - 向量检索（Chroma）
  - 关键词检索（ElasticSearch）
  - RRF融合算法
  - 结果重排序

- [ ] 专业词库集成
  - 词库数据结构
  - 词库管理接口
  - 查询扩展功能

**交付物**：
- 完整的知识库管理API
- 混合检索功能
- 文档处理管线

### Phase 3（1.5周）：意图识别与质量保障

**目标**：实现意图识别和答案质量保障

- [ ] 意图识别模块
  - 意图配置管理
  - 基于LLM的意图识别
  - 实体提取（NER）
  - 意图路由

- [ ] 敏感词过滤
  - 敏感词库管理
  - 输入敏感词检测
  - 输出敏感词过滤
  - 变体检测

- [ ] 答案质量评估
  - 置信度计算
  - 质量阈值配置
  - 低质量答案处理

- [ ] 对话记录管理
  - 对话记录存储
  - 对话记录查询
  - 对话统计分析

**交付物**：
- 意图识别功能
- 敏感词过滤系统
- 答案质量保障机制

### Phase 4（1.5周）：系统集成与优化

**目标**：系统集成、性能优化、上线准备

- [ ] 任务编排服务
  - 异步任务队列（Celery）
  - 后台作业调度
  - 任务进度查询

- [ ] Docker容器化
  - Dockerfile编写
  - docker-compose.yml配置
  - 容器网络配置

- [ ] 接口优化
  - 接口限流
  - 请求缓存
  - 批处理优化

- [ ] 监控告警
  - 日志收集
  - 性能监控
  - 错误告警

- [ ] 前后端联调
  - API文档完善
  - 前端联调支持
  - 问题修复

**交付物**：
- Docker部署方案
- 完整的监控系统
- 生产级API服务

---

## 六、风险与挑战

### 1. 技术风险

- **LLM API稳定性**：
  - 风险：OpenAI API可能不稳定或限流
  - 应对：实现多模型备份方案，支持DeepSeek/Qwen

- **向量检索准确性**：
  - 风险：向量检索召回率不足
  - 应对：混合检索策略（向量+关键词）

- **性能瓶颈**：
  - 风险：高并发下性能不足
  - 应对：缓存优化、异步处理、水平扩展

### 2. 业务风险

- **知识库质量**：
  - 风险：知识库内容不准确或过时
  - 应对：建立知识库质量管理流程

- **用户满意度**：
  - 风险：回答质量不满足用户期望
  - 应对：持续优化Prompt、知识库和检索策略

### 3. 成本风险

- **LLM调用成本**：
  - 风险：高频调用导致成本过高
  - 应对：缓存机制、批处理、选用性价比高的模型

---

## 七、总结

本文档基于原始需求，进行了全面的补充和完善，涵盖：

1. **对话引擎模块**：补充了接口鉴权、限流、异常处理、个性化等需求
2. **知识库系统**：细化了文档处理、混合检索、质量管理等需求
3. **对话记录管理**：新增了数据分析、审计、回放等需求
4. **词库管理**：扩展了词库分类、版本管理、权限管理等需求
5. **意图识别模块**：补充了实体提取、意图路由、优化工具等需求
6. **技术架构**：补充了LangGraph工作流、数据库设计、性能要求等
7. **开发计划**：明确了4个阶段的开发里程碑和交付物

这些补充需求使得项目更加完整和可落地，为后续开发提供了清晰的指导。
