# Standalone ChromaDB Testing Requirements
# 
# This is a minimal requirements file for testing ChromaDB in isolation
# without installing all the dependencies of the main project.
#
# Usage:
#   python -m venv venv_chroma_test
#   source venv_chroma_test/bin/activate  # or venv_chroma_test\Scripts\activate on Windows
#   pip install -r requirements-chroma-test.txt
#   python test_chroma_standalone.py

# Core dependencies for ChromaDB testing
chromadb==0.6.3          # Recommended stable version
# chromadb==0.5.23       # Current project version (alternative)
# chromadb==1.3.7        # Latest version (requires code changes)

# HTTP client for embedding API
requests>=2.31.0

# Environment variable management
python-dotenv>=1.0.0

# Optional: For better testing output
pytest>=7.4.0           # If you want to convert to pytest format
pytest-asyncio>=0.21.0  # For async test support

# Note: You can choose different chromadb versions for testing:
#
# Stability Priority:     chromadb==0.6.3   (Recommended)
# Current Compatibility:  chromadb==0.5.23  (Exact match with project)
# Latest Features:        chromadb==1.3.7   (Requires code changes)
